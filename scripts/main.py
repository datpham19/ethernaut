from scripts.deploy import deploy_fallback, deploy_fallout, deploy_coinflip, deploy_telephone, deploy_token, \
    deploy_delegation, deploy_force, deploy_vault, deploy_king, deploy_re_entrance, \
    deploy_elevator, deploy_privacy, deploy_gatekeeper_one, deploy_gatekeeper_two, deploy_naught_coin, deploy_preservation, deploy_magic_number


# NOTES
# deploy_contract is used for when the instance is either from address or deployed locally, depending on network
# ClassName.deploy() is used when we want to explicitly deploy it regardless of network
def main():
    # deploy_fallback()
    # deploy_fallout()
    # deploy_coinflip()
    # deploy_telephone()
    # deploy_token()
    deploy_delegation()
    # deploy_force()
    # deploy_vault()
    # deploy_king()
    # deploy_re_entrance()
    # deploy_elevator()
    # deploy_privacy()
    # deploy_gatekeeper_one()
    # deploy_gatekeeper_two()
    # deploy_naught_coin()
    # deploy_preservation()
    # deploy_magic_number()
